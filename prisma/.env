# Prisma CLI loads this file automatically. For Supabase, run commands with:
#   npx dotenv -e .env.local -- prisma db push
# Next.js / npm run dev use DATABASE_URL from .env.local at the project root.
